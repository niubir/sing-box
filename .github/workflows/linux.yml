# publish.yml
name: Publish
on:
  push:
    tags:
      - 'v*.*.*'
jobs:
  release:
    name: Publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ^1.23

      - name: Install
        run: |
          go mod tidy

      - name: Build Windows
        run: |
          CGO_ENABLED=0 GOOS=windows GOARCH=386 go build -o sing-box.exe ./cmd/sing-box
          zip sing-box-windows-386.zip sing-box.exe

          CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -o sing-box.exe ./cmd/sing-box
          zip sing-box-windows-amd64.zip sing-box.exe

          CGO_ENABLED=0 GOOS=windows GOARCH=arm64 go build -o sing-box.exe ./cmd/sing-box
          zip sing-box-windows-arm64.zip sing-box.exe

      - name: Build Darwin
        run: |
          CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-darwin-amd64.tar.gz sing-box

          CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-darwin-arm64.tar.gz sing-box

      - name: Build Linux
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=386 go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-linux-386.tar.gz sing-box

          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-linux-amd64.tar.gz sing-box

          CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-linux-arm64.tar.gz sing-box

          CGO_ENABLED=0 GOOS=linux GOARCH=arm GOARM=6 go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-linux-armv6.tar.gz sing-box

          CGO_ENABLED=0 GOOS=linux GOARCH=arm GOARM=7 go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-linux-armv7.tar.gz sing-box

          CGO_ENABLED=0 GOOS=linux GOARCH=mips64le go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-linux-mips64le.tar.gz sing-box

          CGO_ENABLED=0 GOOS=linux GOARCH=riscv64 go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-linux-riscv64.tar.gz sing-box

          CGO_ENABLED=0 GOOS=linux GOARCH=s390x go build -o sing-box ./cmd/sing-box
          chmod +x sing-box
          tar -czf sing-box-linux-s390x.tar.gz sing-box

      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            *.zip
            *.tar.gz